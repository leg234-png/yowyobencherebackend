# ===========================================
# CONFIGURATION SIMPLIFIÉE - SERVICE ENCHÈRES
# ===========================================

# --- SERVICE INFO ---
spring.application.name=yowyob-enchere-backend-reactive
server.port=8031
spring.webflux.base-path=/api

# --- BASE DE DONNÉES R2DBC (RÉACTIVE) ---
spring.r2dbc.url=r2dbc:mysql://157.90.26.3:3307/enchereyowyob?serverTimezone=UTC
spring.r2dbc.username=${DB_USERNAME:root}
spring.r2dbc.password=${DB_PASSWORD:Azerty123}
spring.r2dbc.pool.enabled=true
spring.r2dbc.pool.initial-size=5
spring.r2dbc.pool.max-size=20

# --- FLYWAY (MIGRATIONS DB) ---
spring.datasource.url=jdbc:mysql://157.90.26.3:3307/enchereyowyob?serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Azerty123}
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# --- JWT CUSTOM (vous avez CustomReactiveJwtDecoder) ---
app.jwt.secret-key=${JWT_SECRET:faceb782375b50f9d24e043beeca80fae093c934cbf0ade576f4b36258de6b44}

# --- SERVICES EXTERNES ---
app.auth-service.base-url=${AUTH_SERVICE_URL:http://157.90.26.3:8032/api}
app.user-service.base-url=${USER_SERVICE_URL:http://157.90.26.3:8032/api}

# --- UPLOAD FILES ---
app.upload.path=${UPLOAD_PATH:uploads/}
app.upload.base-url=${UPLOAD_BASE_URL:http://157.90.26.3:8031/api/uploads/}

# --- MODE DÉVELOPPEMENT ---
app.dev.bypass-external-validations=${DEV_MODE:false}

# --- SWAGGER/OpenAPI ---
springdoc.api-docs.path=/api-docs

spring.main.allow-bean-definition-overriding=true